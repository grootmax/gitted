{
  "name": "gitted",
  "version": "1.0.0",
  "description": "Dynamic fan-out dampening, inverse reference frequency scoring, regex fallback parser and parse status indicator for Context Builder",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [
    "static-analysis",
    "code-graph",
    "ast-parser",
    "fallback-parser",
    "context-builder",
    "scoring-engine",
    "fan-out-dampening"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.1"
  }
}
